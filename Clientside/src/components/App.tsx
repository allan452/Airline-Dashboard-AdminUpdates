import React from "react";
import DisplayProducts from "./DisplayProducts/Display-Products";

const App: React.FC = () => (
  <div>
    <h1>React + TypeScript + SCSS 🚀</h1>
    <DisplayProducts />
  </div>
);

export default App;
